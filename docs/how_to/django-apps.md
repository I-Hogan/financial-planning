# Django Apps

To add a new Django app, create the app package and register it in
`config/settings.py` under `INSTALLED_APPS`.

Example entry:

```python
INSTALLED_APPS = [
    # ...
    'financial_planner',
]
```
